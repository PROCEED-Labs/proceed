{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["**/@types"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
