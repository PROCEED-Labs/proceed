const config = require('./configuration/configHandler');
const logging = require('./logging/logging.js');
const information = require('./information/machineInformation');

/**
 * @module @proceed/machine
 */
module.exports = {
  config,
  logging,
  information,
};
