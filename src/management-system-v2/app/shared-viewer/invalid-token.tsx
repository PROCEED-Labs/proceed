'use client';

import { Typography } from 'antd';

const InvalidShareToken = () => {
  return (
    <div>
      <Typography.Text type="danger"> Invalid Token</Typography.Text>
    </div>
  );
};

export default InvalidShareToken;
