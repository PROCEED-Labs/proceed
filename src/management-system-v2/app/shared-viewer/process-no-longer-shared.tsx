'use client';
import { Typography } from 'antd';

const ProcessNoLongerShared = () => {
  return <Typography.Text type="danger">Process no longer shared</Typography.Text>;
};

export default ProcessNoLongerShared;
